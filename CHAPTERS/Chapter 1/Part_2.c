#include <stdio.h>

int main()
// going to start my first program
{
    int a = 4;
    // int b = 8.8; // not recommended as 8.8 is not an integer
    float b = 8.8;
    char c = 'h';
    int d = 25;
    int e = 25 + 4;
    printf("the value of a is %d %d \n", a, a);
    printf("the value of b is %f \n", b);
    printf("the value of c is %c \n", c);
    printf("the sum of a and b is %d \n", a + d);
    printf("the sum of a and b is %d \n", e);
    return 0;
}
// happily ended