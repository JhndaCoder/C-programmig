// To calculate area of a square using function

#include <stdio.h>
int area(int a, int b);

int main()
{
    int Area, a, b;
    a == b;
    printf("Enter the value of edge of square\n");
    scanf("%d", &a);
    Area = area(a, b);
    printf("The area of square is %d", Area);
    return 0;
}
int area(int a, int b)
{
    int x;
    x = a * a;
}