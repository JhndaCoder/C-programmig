// to convert celsius to farenheit
#include <stdio.h>

int main()
{
    float celsius = 3;
    float far = (celsius * 9 / 5) + 32;
    printf("the value of this temperataure in farenheit is %f", far);
    return 0;
}